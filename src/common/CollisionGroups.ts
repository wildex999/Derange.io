export class CollisionGroups {
    public static PLAYER = Math.pow(2, 0);
    public static TILE = Math.pow(2, 1);
    public static ENEMY = Math.pow(2, 2);
    public static ATTACK = Math.pow(2, 3);

}